<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aitrade.exchange.mapper.TradeRecordMapper">

    <insert id="insert" parameterType="TradeRecord" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO trade_record (
            symbol, trade_type, entry_index, exit_index,
            entry_price, exit_price, amount,
            entry_time, exit_time, reason, remark
        ) VALUES (
                     #{symbol}, #{tradeType}, #{entryIndex}, #{exitIndex},
                     #{entryPrice}, #{exitPrice}, #{amount},
                     #{entryTime}, #{exitTime}, #{reason}, #{remark}
                 )
    </insert>

    <select id="findBySymbol" resultType="TradeRecord">
        SELECT * FROM trade_record
        WHERE symbol = #{symbol}
        ORDER BY entry_time ASC
    </select>

    <select id="findOpenPosition" resultType="TradeRecord">
        SELECT * FROM trade_record
        WHERE symbol = #{symbol} AND exit_time IS NULL
        ORDER BY entry_time DESC LIMIT 1
    </select>

    <select id="findLastEntry" resultType="TradeRecord">
        SELECT * FROM trade_record
        WHERE symbol = #{symbol} AND trade_type = 'ENTRY'
        ORDER BY entry_time DESC LIMIT 1
    </select>
</mapper>